# Stricter than the flutter_lints template on purpose: CI runs
# `flutter analyze --fatal-infos`, so every rule here is a hard gate.
# Generated code (drift *.g.dart, gen-l10n output) is excluded rather than
# annotated.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - lib/l10n/app_localizations*.dart
    - lib/**/*.g.dart
  # No implicit dynamic: every inferred type must resolve.
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    # Style rules that dart fix can apply mechanically.
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_redundant_argument_values: true
    avoid_unused_constructor_parameters: true
    cancel_subscriptions: true
    close_sinks: true
    directives_ordering: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_in_for_each: true
    prefer_single_quotes: true
    # Fire-and-forget futures must say so (haptics, background refresh).
    unawaited_futures: true
    unnecessary_await_in_return: true
    unnecessary_lambdas: true
    unnecessary_parenthesis: true
    use_super_parameters: true
